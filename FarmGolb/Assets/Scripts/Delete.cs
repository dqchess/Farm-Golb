using UnityEngine;
using System.Collections;
using System;

namespace PlatformCore
{
    public class Delete : MonoBehaviour
    {
        private void Start()
        {
            //Employee emp = new Employee("tuan", 1);
            //print(emp.Name);
        }

        private void Update()
        {
            //print(string.Format("order = {0} / pos = {1}", Mathf.RoundToInt((40 - transform.position.y) * 100f), GameManager.Instance.getCellTileMap(transform.position)));
            //
            //
        }
    }

    class Employee
    {
        //public Employee(string s, int i) => (Name, Id) = (s, i);
        //public string Name { get; set; }
        //public int Id { get; set; }
        //

    }
}

